services:
  blog_dashboard:
    container_name: blog_dashboard
    build: .
    restart: always
    networks:
      - my_network
    ports:
      - "8081:80"
      - "444:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro

networks:
  my_network:
    driver: bridge
